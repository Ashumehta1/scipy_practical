import numpy as np
#from scipy.linalg import solve,inv,det,eig,svd,norm
from scipy import linalg
print('''
    solve(A, b) → regression, optimization
    inv(A) → theory of regression equations
    det(A) → check invertibility, probability density functions
    eig(A) → PCA, dimensionality reduction
    svd(A) → PCA, recommender systems, image compression
    norm(A) → distance metrics, loss functions, regularization''')
